# -*- mode: snippet -*-
# name: buster.testCase
# key: tc
# --
${1:if (typeof require === "function" && typeof module !== "undefined") {
  buster = require("buster");
}

}(function ($3) {
  "use strict";
  var assert = buster.assert;

  buster.testCase('${4:`(chop-suffix " Test" (capitalized-words (buffer-file-name-body)))`}', {
    tt$0
  });

}(${2:namespace}));