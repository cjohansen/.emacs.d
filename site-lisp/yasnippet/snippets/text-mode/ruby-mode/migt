class Create${1:`(substring (upper-camel-case(buffer-file-name-body)) 9 -5)`}Table
  def up(db)
    db.query <<-SQL
CREATE TABLE $1 (
  ${2:id                MEDIUMINT UNSIGNED   NOT NULL AUTO_INCREMENT,
  $0
  PRIMARY KEY (id)}
);
SQL
  end

  def down(db)
    db.query "DROP TABLE IF EXISTS $1"
  end

end
